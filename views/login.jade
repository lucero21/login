//Created by lucero on 29/02/2016.
extends layout

block content
  center
    .container
        h1 Iniciar sesión
        p.lead Para continuar ingresa los siguientes datos:
        br
        form(role='form', action="/login",method="post", style='max-width: 300px;')
            .form-group
                input.form-control(type='text', name="username", placeholder='Usuario')
            .form-group
                input.form-control(type='password', name="password", placeholder='Contraseña')
            button.btn.btn-default(type='submit') Enviar
            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
            a(href='/')
              button.btn.btn-primary(type="button") Cancelar
        p ___________________________________________________

    if (!user)
        br
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
        a.registrar(href="/register") Registrar nuevo Usuario